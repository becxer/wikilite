<header>
  <!--디렉토리 이름  -->
  <nav class="top-nav teal">
    <div class="container">
      <div class = "nav-wrapper">
      <a class="page-title" style="font-size:48px"> 
        <%= title %>
      </a> 
      </div>
    </div>
  </nav>

  <!--슬라이드네비 on/off 버튼 -->
  <div class="container">
    <a href="#" data-activates="nav-mobile" class="button-collapse top-nav full">
      <i class="mdi-navigation-menu"></i>
    </a>
  </div>

  <!--슬라이드네비 디렉토리 리스트 -->
    <ul id="nav-mobile" class="side-nav fixed">
        <li class="logo no-padding" >
      <a id="logo-container" href="/" class="brand-logo"> 
        <img src="/img/logo.png" />
      </a>
    </li>

      <% for (i in dirs) { %>
        <li class="list bold"><a href='/<%=dirs[i].name%>' class="waves-effect waves-teal"><%=dirs[i].name%></a></li>

        <% if (dirs[i].name == title) { %>
          <ul>
            <% for (i in filters) { %>  
              <li class="list"><a href="<%=filters[i].urlpath%>" class="filter"><%=filters[i].name%></a></li>
            <% } %>
          </ul>
        <% } %>

      <% } %>

        <li>
            <fb:login-button scope="public_profile,email" onlogin="checkLoginState();"></fb:login-button>
        </li>
    </ul>
</header>
